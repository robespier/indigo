#target bridge

#include 'indigo-utils.jsxinc'

var reply = "";
var conn = new Socket();
conn.timeout = 1;
conn.encoding = "UTF-8";

var parcel = {
	ink: 36,
	roll: 2,
	template: "133400",
	print_list : ["One","Tw;?o","Два с половиной"]
};

if (conn.open ("indigo.aicdr.pro:8080")) {
	var jb = new Indigo.JsonBroker();
	var jinson = jb.encode(parcel);
	conn.write(jinson);
	var jsonReply = jb.decode(conn.read());
	conn.close();
}
